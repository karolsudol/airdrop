// // import { HardhatRuntimeEnvironment } from "hardhat/types";
// import { ethers } from "hardhat";

// async function hash(account: string, amount: string) {
//   return Buffer.from(
//     ethers.utils
//       .solidityKeccak256(["address", "uint256"], [account, amount])
//       .slice(2),
//     "hex"
//   );
// }

// export default hash;
